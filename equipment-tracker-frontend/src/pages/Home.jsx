import { Container, Typography } from "@mui/material";

const Home = () => {
  return (
    <Container>
      <Typography variant="h4">Welcome to Equipment Tracker</Typography>
    </Container>
  );
};

export default Home;